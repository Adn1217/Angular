import { RoundedBlockDirective } from './rounded-block-directive.directive';

describe('RoundedBlockDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedBlockDirective();
    expect(directive).toBeTruthy();
  });
});
